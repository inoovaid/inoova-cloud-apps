apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: openclaw
data:
  openclaw.json: |
    {
      "gateway": {
        "bind": "0.0.0.0",
        "port": 18789,
        "auth": {
          "mode": "token",
          "token": "${OPENCLAW_GATEWAY_TOKEN}"
        }
      },
      "models": {
        "providers": {
          "ollama": {
            "api": "ollama",
            "baseUrl": "http://ollama.ai-system:11434",
            "models": {
              "default": {
                "model": "qwen2.5-coder:7b",
                "contextWindow": 32000,
                "contextTokens": 25000,
                "maxTokens": 8000
              }
            }
          }
        },
        "defaults": {
          "provider": "ollama"
        }
      },
      "agents": {
        "defaults": {
          "memorySearch": {
            "provider": "ollama"
          }
        }
      }
    }
  
  AGENTS.md: |
    # OpenClaw Agents
    
    ## Default Agent
    - id: default
      name: OpenClaw Assistant
      description: Your personal AI assistant powered by Ollama
      provider: ollama
      model: qwen2.5-coder:7b
