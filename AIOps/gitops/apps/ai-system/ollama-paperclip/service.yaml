apiVersion: v1
kind: Service
metadata:
  name: ollama-paperclip
  namespace: ai-system
spec:
  type: ClusterIP
  selector:
    paperclip-llm: enabled
  ports:
    - name: http
      port: 11434
      targetPort: 11434
      protocol: TCP
