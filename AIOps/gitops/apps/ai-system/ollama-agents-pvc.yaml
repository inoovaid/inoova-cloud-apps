apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-agents-pvc
  namespace: ai-system
spec:
  storageClassName: ollama-models
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
