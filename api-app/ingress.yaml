apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-app
spec:
  ingressClassName: nginx
  rules:
  - host: api-cloud.dnn.lat
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-app
            port:
              number: 80
