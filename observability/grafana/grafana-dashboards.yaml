apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  loki-logs.json: |
{{ .Files.Get "dashboards/loki-logs.json" | indent 4 }}
